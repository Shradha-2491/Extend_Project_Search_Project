<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Product_Search_Project">
        <!--
          allowBackup is explicitly false: Android auto-backup would otherwise
          copy app-private storage (including the EncryptedSharedPreferences
          file, which is encrypted at rest but whose *key* lives in the
          Android Keystore and does NOT travel with a backup) to the user's
          Google account -- disabling it removes that class of insecure
          local-storage exposure entirely rather than relying on encryption
          alone. See docs/mobile-security-checklist.md.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Product_Search_Project"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AppAuth redirects here after the Google Sign-In Custom Tab
             completes; the scheme matches manifestPlaceholders.appAuthRedirectScheme
             in app/build.gradle.kts. -->
        <activity
            android:name="net.openid.appauth.RedirectUriReceiverActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="${appAuthRedirectScheme}" android:host="oauth2redirect" />
            </intent-filter>
        </activity>
    </application>

</manifest>